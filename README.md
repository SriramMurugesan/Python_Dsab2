# Python_Dsab2
git add .
git commit -m "day1"
git push

what are types of errors:
valueerror
zerodivision error
typerror
nameerror
indexerror
keyerror

try:
    pass
except:
    pass
except:
    pass
else:
    pass
finally:
    pass
file handle
with open("filename","w,r,a,x") as f:
    f.write("hello")
    f.read()
oops
encapsulation--> hiding the data using access modifiers
private--> __balance (can be accessed inside of class)
public--> name (can be accessed inside and outside of class)
protected--> _name (can be accessed inside and inside of child class)

inheritance--> acquiring the properties of parent class
polymorphism--> one name multiple forms
abstractio --> forcing child class implement abstract method